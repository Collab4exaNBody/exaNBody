# ====================================================================
# === micro-stamp pair potential compute operator library library  ===
# ====================================================================

set(exanbDefBox_LINK_LIBRARIES exanbCompute)
xstamp_add_plugin(exanbDefBox ${CMAKE_CURRENT_SOURCE_DIR})

