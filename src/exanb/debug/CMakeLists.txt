# =================================
# === micro-stamp debug library ===
# =================================

set(exanbDebug_LINK_LIBRARIES exanbParticleNeighbors exanbMPI)
xstamp_add_plugin(exanbDebug ${CMAKE_CURRENT_SOURCE_DIR})

